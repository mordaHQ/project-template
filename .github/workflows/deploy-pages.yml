name: Build and Deploy Docs (Cloudflare Pages)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      # 1. Checkout project
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Node.js 20 (Docusaurus requires >=20)
      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      # 3. Install root dependencies
      - name: Install root dependencies
        run: npm ci

      # 4. Install Docusaurus dependencies
      - name: Install Docusaurus deps
        run: |
          cd docusaurus-build
          npm ci --legacy-peer-deps

      # 5. Type-check
      - name: Type check Docusaurus
        run: |
          cd docusaurus-build
          npm run typecheck

      # 6. Generate API docs
      - name: Generate API docs (TypeDoc)
        run: npm run docs:api

      # 7. Build website
      - name: Build Docusaurus site
        run: |
          cd docusaurus-build
          npm run build

      # 8. Upload artifact for Cloudflare Pages
      - name: Upload Pages build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docusaurus-build/build

      # 9. Deploy ONLY on main
      - name: Deploy to Cloudflare Pages
        if: github.ref == 'refs/heads/main'
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: ${{ secrets.CLOUDFLARE_PROJECT_NAME }}
          directory: docusaurus-build/build
